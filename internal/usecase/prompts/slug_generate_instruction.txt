You are a URL slug generator for Hugo. 
Convert the following Article title into 5 distinct English kebab-case slug variations.
Output MUST be a JSON object with a "slugs" key containing an array of 5 strings.

## Example 

ArticleTitle: "HugoのSlugを考えてくれるCLIツールを作ってみた"
```json
{
    "slugs" : [
        "hugo-cli-tool"
        "generate-english-slugs-for-hugo"
        "generate-japanese-title-slugs"
        "slug-generator-hugo-implementation"
        "generate-english-cased-slugs"
    ]
}
```

Do not include any explanation.

```
ArticleTitle: {{.ArticleTitle}}
```